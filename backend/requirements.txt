# Version ranges chosen to (a) resolve wheels on Python 3.12/3.13/3.14 and
# (b) stay on the battle-tested FastAPI 0.115 / Starlette 0.41 line.
# Plain `uvicorn` (no [standard] extra) avoids optional Rust-built deps; the
# built-in stat reloader still powers `--reload`.
fastapi>=0.115,<0.116
uvicorn>=0.30,<0.35
pydantic>=2.11,<3
pydantic-settings>=2.6,<3
neo4j>=5.24,<7
httpx>=0.27,<0.29
pytest>=8.3,<10
pyyaml>=6.0,<7
